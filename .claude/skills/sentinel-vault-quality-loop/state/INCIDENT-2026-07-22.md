# Incident evidence — sealed-media delete + resize (observed 2026-07-22, reported 2026-08-14)

Page: WORK FOR HIRE Home (`852172`, space WFH `851971`). Objects: `preview.png` = `att287211521` (fileId `91429f4d-5ac6-493d-b443-34664e96388c`), `modular.jpeg` = `att287473678`, `10619538101.pdf` = `att216989697`.
Actors: Gabriela `712020:2b9d007d…` (seal owner), LeanZero SRL (office) `712020:cecf4c53…` (the "attacker"), dev-install app account `712020:1c64a8bc…` (confirmed via KVS `app-account-id`).

## Established facts (all REST-verified 2026-08-14)

1. **All violation/seal comments were authored by ONE app account (`1c64a8bc`, the dev install).** Dual-install double-posting is RULED OUT. The wolfaenpak prod install (skill said `a04a7780`, v2) is ALREADY GONE — `forge install list` shows only dev on wolfaenpak (+ prod on test-easy-apps.atlassian.net, v3, untouched).
2. **Bug A root cause CONFIRMED — the trash handler did not restore; a human did.** preview.png attachment v2 (the un-trash) was created at 12:14:15 by **Gabriela**, not the app. Sequence: gabriela uploads+seals (12:06), office deletes via UI (attachment → trash + page node removed), `handleSealedArtifactTrash` silently skipped (no app-authored attachment version exists; prime suspect = the `attachment.version.number`-missing / event-shape precondition at triggers.js:1147, possibly event-not-delivered — July logs are beyond forge-logs retention, so the UI-delete path must be exercised empirically by the new harness spec). Meanwhile `restoreMediaPass` DID run (app-authored page versions v25/v27) and re-spliced a DEAD media node pointing at the trashed attachment + posted "The page content has been reverted." — false while the file sat in trash. Realm sealed-files list kept showing the seal (no existence probe). Gabriela manually restored from trash (att v2) and re-sealed (12:15).
3. **Comment spam mechanism CONFIRMED**: violation pairs (pdf + preview, same second) at 12:17:49-50 and 12:21:52-53 = two app restore runs ~4 min apart. Office's stale editor draft kept dropping the sealed nodes on each publish → each trigger run re-restored + re-commented. No cross-run dedup exists (Fix 3).
4. **Resize (Bug B)**: modular.jpeg resized to 138px display width — saved, never reverted (attachment seals are fileId-presence-only; presentation attrs invisible). Owner decided STRICT semantics → Fix 6.
5. **The attachment-EDIT revert path WORKS**: office uploaded modular.jpeg v3 at 13:13:31 → app-authored revert v4 at 13:13:45 + "attempted to edit" comment. Healthy.
6. **Current page state self-healed** (via gabriela's manual restore + office's manual version reverts v42/v43): ADF v45 contains the preview.png node with the CORRECT current fileId; attachment status current. fileId survives trash+restore (both manual and, per API semantics, PUT-based).
7. **API semantics probe (gates the Fix-1 classifier; probed live on a throwaway attachment)**:
   - v2 `GET /api/v2/attachments/{id}`: current → 200 `status:"current"`; trashed → **200 `status:"trashed"` WITH pageId, version.number, title** (everything restore needs in one call); purged → **404 NOT_FOUND**.
   - v1 `GET /rest/api/content/{id}?status=trashed`: 200 `status:"trashed"` but container/version absent in default expand; after purge → 404.
   - v1 child-attachment listing with `?status=trashed` IGNORES the filter (returns current items) — do not use it.
   - ⇒ `probeAttachmentStatus` = a single v2 GET: 200→current/trashed (with restore inputs), 404→deleted, 429/5xx after retry→unknown.
8. Old copy bug visible in ancient comment 217219073: "locked by ." (empty name) — historical, superseded copy path.
9. modular.jpeg seal record persists with `expiresAt` 2026-07-26 (expired-inert, owner nudged via "Seal Expired" comments — by design), while preview.png's record is gone. Expired-but-listed contributes to "still appears in sealed files" perception.

## Fix implications

- Fix 2 (trash-handler fetch-and-proceed) + Fix 1 (unified restore) directly close Bug A; the v2 GET carries version+pageId, so the version-less event shape is fully recoverable.
- Fix 3 (cross-run dedup marker) closes the comment spam.
- Fix 6 (strict attr baseline, shadow-first) closes the resize gap.
- Dual-install action: none needed (already resolved); keep the deploy-state guard spec so staleness is caught structurally.
